Class Service
=============

This Class is DEPRECATED since version ``0.26.0``.
It is no longer recommended for use and may be removed in the future.

Use :py:mod:`ooodev.loader.inst.service` instead.

.. code-block:: python

    from ooodev.loader.inst.service import Service

    # other code